Add your Android alert file here as `ride_request_alert.mp3`, then copy the same file to:

`apps/driver/android/app/src/main/res/raw/ride_request_alert.mp3`

The foreground ride-request bottom sheet and Android push channel are already wired to use the sound name `ride_request_alert`.
