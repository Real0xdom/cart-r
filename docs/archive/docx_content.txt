<p><strong>CARTR</strong></p><p>Bug Report &amp; Developer Task Brief</p><p>Prepared by: Advaita AI Systems  |  Date: 9 March 2026  |  Total Issues: 20</p><h2><strong>Priority Legend</strong></h2><table><tr><td><p><strong>🔴 CRITICAL</strong></p></td><td><p><strong>🟠 HIGH</strong></p></td><td><p><strong>🔵 MEDIUM</strong></p></td><td><p><strong>🟢 LOW</strong></p></td></tr></table><h2><strong>Issue Summary</strong></h2><table><thead><tr><th><p><strong>ID</strong></p></th><th><p><strong>Issue Title</strong></p></th><th><p><strong>Priority</strong></p></th><th><p><strong>Module</strong></p></th></tr></thead><tbody><tr><td><p><strong>BUG-001</strong></p></td><td><p>Duplicate Email Error — No User-Friendly UI Feedback</p></td><td><p><strong>HIGH</strong></p></td><td><p>Driver App</p></td></tr><tr><td><p><strong>BUG-002</strong></p></td><td><p>Replace Emojis with Professional Icons</p></td><td><p><strong>MEDIUM</strong></p></td><td><p>Driver App</p></td></tr><tr><td><p><strong>BUG-003</strong></p></td><td><p>Input Validation Missing on Registration Form</p></td><td><p><strong>HIGH</strong></p></td><td><p>Driver App</p></td></tr><tr><td><p><strong>BUG-004</strong></p></td><td><p>Vehicle Details — Dropdowns + Date Picker</p></td><td><p><strong>HIGH</strong></p></td><td><p>Driver App</p></td></tr><tr><td><p><strong>BUG-005</strong></p></td><td><p>Document Upload — Cache on Back Navigation</p></td><td><p><strong>HIGH</strong></p></td><td><p>Driver App</p></td></tr><tr><td><p><strong>BUG-006</strong></p></td><td><p>Docs Must Fully Upload Before Verification Redirect</p></td><td><p><strong>CRITICAL</strong></p></td><td><p>Driver + Admin</p></td></tr><tr><td><p><strong>BUG-007</strong></p></td><td><p>Admin: Add Request Resubmission Button</p></td><td><p><strong>HIGH</strong></p></td><td><p>Admin Console</p></td></tr><tr><td><p><strong>BUG-008</strong></p></td><td><p>Push Notifications on Approval/Rejection/Resubmission</p></td><td><p><strong>HIGH</strong></p></td><td><p>Driver App</p></td></tr><tr><td><p><strong>BUG-009</strong></p></td><td><p>Suspended Driver Sees Wrong Screen</p></td><td><p><strong>CRITICAL</strong></p></td><td><p>Driver App</p></td></tr><tr><td><p><strong>BUG-010</strong></p></td><td><p>Pickup Location Inaccurate on Map</p></td><td><p><strong>CRITICAL</strong></p></td><td><p>Driver App</p></td></tr><tr><td><p><strong>BUG-011</strong></p></td><td><p>Map Doesn't Show Route to Drop Location</p></td><td><p><strong>HIGH</strong></p></td><td><p>Driver App</p></td></tr><tr><td><p><strong>BUG-012</strong></p></td><td><p>QR Code Payment Must Use CashFree In-App</p></td><td><p><strong>CRITICAL</strong></p></td><td><p>Driver App</p></td></tr><tr><td><p><strong>BUG-013</strong></p></td><td><p>OTP Verification Too Slow + No Retry Logic</p></td><td><p><strong>HIGH</strong></p></td><td><p>Driver App</p></td></tr><tr><td><p><strong>BUG-014</strong></p></td><td><p>App Stuck After Delivery OTP Accepted</p></td><td><p><strong>HIGH</strong></p></td><td><p>Driver App</p></td></tr><tr><td><p><strong>BUG-015</strong></p></td><td><p>Rides Tab — Show History + Ongoing + New Requests</p></td><td><p><strong>MEDIUM</strong></p></td><td><p>Driver App</p></td></tr><tr><td><p><strong>BUG-016</strong></p></td><td><p>Customer App Shows 'In Progress' After Completion</p></td><td><p><strong>HIGH</strong></p></td><td><p>Customer App</p></td></tr><tr><td><p><strong>BUG-017</strong></p></td><td><p>Previous Trip Details Pre-filled in New Booking</p></td><td><p><strong>HIGH</strong></p></td><td><p>Customer App</p></td></tr><tr><td><p><strong>BUG-018</strong></p></td><td><p>No Floating Notification for New Ride Requests</p></td><td><p><strong>HIGH</strong></p></td><td><p>Driver App</p></td></tr><tr><td><p><strong>BUG-019</strong></p></td><td><p>Accepting from Notification Doesn't Show Route</p></td><td><p><strong>HIGH</strong></p></td><td><p>Driver App</p></td></tr><tr><td><p><strong>BUG-020</strong></p></td><td><p>Payment Completion Flow — Multi-Step Required</p></td><td><p><strong>CRITICAL</strong></p></td><td><p>Driver App</p></td></tr></tbody></table><h1><strong>Detailed Issue Specifications</strong></h1><p><strong>  BUG-001  </strong>  <strong>[HIGH]  Duplicate Email Error — No User-Friendly UI Feedback</strong></p><table><tr><td><p><strong>Module / Screen</strong></p></td><td><p>Driver App → Registration Screen</p></td></tr><tr><td><p><strong>Priority</strong></p></td><td><p>HIGH — Fix in next sprint</p></td></tr><tr><td><p><strong>Summary</strong></p></td><td><p>When a driver tries to register with an already-existing email, the app shows a generic or no error. The user gets confused and may attempt multiple re-registrations.</p></td></tr></table><p><strong>Problem Statement</strong></p><ul><li>Supabase returns a duplicate email error but it is not caught and displayed properly</li><li>No visual indicator (toast, inline message, or banner) tells the user their email is already registered</li><li>User experience breaks at a critical onboarding step</li></ul><p><strong>Developer Tasks</strong></p><ul><li>Catch the specific Supabase auth error code for duplicate email (AuthApiError: User already registered)</li><li>Display an inline error below the email input field with red border highlight and icon</li><li>Show a user-friendly message: 'An account with this email already exists. Please log in instead.'</li><li>Include a tappable 'Log in' link within the error message that redirects to the login screen</li><li>Add a short toast/snackbar confirmation so the error is unmissable on all screen sizes</li></ul><p><strong>Reference / Notes</strong></p><p><em>Reference: Supabase error codes — https://supabase.com/docs/reference/javascript/auth-signup</em></p><p><strong>  BUG-002  </strong>  <strong>[MEDIUM]  Replace Emojis with Professional Icons in Driver App</strong></p><table><tr><td><p><strong>Module / Screen</strong></p></td><td><p>Driver App → Registration Screen + All Screens</p></td></tr><tr><td><p><strong>Priority</strong></p></td><td><p>MEDIUM — Fix within 2 sprints</p></td></tr><tr><td><p><strong>Summary</strong></p></td><td><p>Various screens use emoji characters as UI icons (e.g., 📷, 🚗, 📄). This looks unprofessional and renders inconsistently across Android/iOS versions.</p></td></tr></table><p><strong>Problem Statement</strong></p><ul><li>Emojis render differently across devices and OS versions</li><li>Emojis cannot be styled (color, size, weight) consistently with the rest of the UI</li><li>Appearance is unprofessional in a commercial driver-facing product</li></ul><p><strong>Developer Tasks</strong></p><ul><li>Audit all screens for emoji usage and document each instance with its location and intended meaning</li><li>Replace emojis with icons from an icon library such as lucide-react-native, react-native-vector-icons, or Expo Icons</li><li>Ensure all icons follow the app's design system (consistent size: 20–24px, color: theme primary/secondary)</li><li>For the registration screen specifically: replace camera emoji with a Camera icon, vehicle emoji with a Car icon, document emoji with a FileText icon</li><li>Test rendering on Android 10+, Android 14, iOS 15, and iOS 17</li></ul><p><strong>Reference / Notes</strong></p><p><em>Recommended library: lucide-react-native or @expo/vector-icons (both MIT licensed, well-maintained)</em></p><p><strong>  BUG-003  </strong>  <strong>[HIGH]  Input Validation Missing on Driver Registration Form</strong></p><table><tr><td><p><strong>Module / Screen</strong></p></td><td><p>Driver App → Registration Screen</p></td></tr><tr><td><p><strong>Priority</strong></p></td><td><p>HIGH — Fix in next sprint</p></td></tr><tr><td><p><strong>Summary</strong></p></td><td><p>The registration form accepts malformed or incomplete data without validation, causing downstream errors in Supabase and confusing the driver.</p></td></tr></table><p><strong>Problem Statement</strong></p><ul><li>Phone number accepts non-numeric characters and invalid lengths</li><li>Email field does not validate format before submission</li><li>Name fields accept special characters and single-character entries</li><li>No feedback is shown to the user when they try to submit with invalid data</li></ul><p><strong>Developer Tasks</strong></p><ul><li>Add inline real-time validation for: email (RFC 5322 format check), phone number (10-digit Indian format, strip +91 if entered), full name (min 3 chars, alpha + spaces only), Aadhaar number (12-digit numeric), PAN card (ABCDE1234F format regex)</li><li>Show inline error messages below each field as the user types or on blur (not only on submit)</li><li>Disable the 'Submit' / 'Continue' CTA button until all fields pass validation</li><li>Show a green checkmark icon inside the field when input is valid</li><li>Sanitize all inputs before sending to Supabase to prevent injection</li></ul><p><strong>Reference / Notes</strong></p><p><em>Use a form validation library like react-hook-form + zod or yup for clean schema-based validation</em></p><p><strong>  BUG-004  </strong>  <strong>[HIGH]  Vehicle Details — Dropdowns for Model &amp; Color + Date Picker for Licence Expiry</strong></p><table><tr><td><p><strong>Module / Screen</strong></p></td><td><p>Driver App → Vehicle Details Screen</p></td></tr><tr><td><p><strong>Priority</strong></p></td><td><p>HIGH — Fix in next sprint</p></td></tr><tr><td><p><strong>Summary</strong></p></td><td><p>The vehicle details form has free-text inputs where structured inputs should be used. Licence expiry date has no calendar picker, and no validation on date/time fields.</p></td></tr></table><p><strong>Problem Statement</strong></p><ul><li>Vehicle model is a free-text field — drivers enter inconsistent values (e.g., 'swift', 'Swift Dzire', 'SWIFT') making admin review unreliable</li><li>Vehicle color is free-text — same inconsistency issue</li><li>Licence expiry date is a text input with no date picker, leading to format errors</li><li>No error shown if a past date is entered for licence expiry</li><li>No error shown for invalid time/date value combinations</li></ul><p><strong>Developer Tasks</strong></p><ul><li>Replace vehicle model input with a searchable dropdown populated from a curated list of Indian commercial vehicles (Tata Ace, Mahindra Bolero Pickup, Ashok Leyland Dost, etc.) — allow 'Other' option with manual entry</li><li>Replace color input with a color picker or dropdown of standard vehicle colors</li><li>Replace licence expiry text input with a native date picker (DateTimePicker from @react-native-community/datetimepicker)</li><li>Add validation: licence expiry must be a future date — show error 'Licence appears to be expired. Please check the date.' if past date is selected</li><li>Add error handling for invalid/null date values: 'Please select a valid date'</li><li>Ensure date is stored in ISO 8601 format (YYYY-MM-DD) in Supabase</li></ul><p><strong>Reference / Notes</strong></p><p><em>For dropdown data: store vehicle models in a Supabase lookup table so admin can update the list without a code release</em></p><p><strong>  BUG-005  </strong>  <strong>[HIGH]  Document Upload — Cache Docs on Back Navigation (Prevent Re-upload Frustration)</strong></p><table><tr><td><p><strong>Module / Screen</strong></p></td><td><p>Driver App → Document Upload Screen</p></td></tr><tr><td><p><strong>Priority</strong></p></td><td><p>HIGH — Fix in next sprint</p></td></tr><tr><td><p><strong>Summary</strong></p></td><td><p>When a driver uploads documents and then presses the back button, all uploaded files are lost. The driver must re-upload everything from scratch — a major UX failure during a tedious process.</p></td></tr></table><p><strong>Problem Statement</strong></p><ul><li>Uploaded document state is not persisted when navigating back</li><li>Driver loses progress if they accidentally tap back or leave the screen briefly</li><li>No visual indication of what has already been uploaded in a session</li></ul><p><strong>Developer Tasks</strong></p><ul><li>Use a global state manager (Redux, Zustand, or React Context) to cache all uploaded document URIs and metadata in app memory as they are selected/uploaded</li><li>On re-entry to the document upload screen, pre-populate already-uploaded documents with a 'Uploaded ✓' status badge</li><li>Do NOT clear document upload state on back navigation — only clear on explicit 'Start Over' action or successful submission</li><li>Persist upload state to AsyncStorage so that even if the app is backgrounded, progress is retained</li><li>Show upload progress (percentage or loading bar) per document so user always knows status</li><li>If a document was already uploaded to Supabase bucket, show its thumbnail/filename from the bucket URL — no need to re-upload</li></ul><p><strong>Reference / Notes</strong></p><p><em>This is a standard pattern in multi-step form flows. Reference: react-navigation's keep-alive screens or a persistent Zustand store</em></p><p><strong>  BUG-006  </strong>  <strong>[CRITICAL]  Document Upload Must Fully Complete Before Redirecting to Verification Screen</strong></p><table><tr><td><p><strong>Module / Screen</strong></p></td><td><p>Driver App → Document Upload → Verification Pending Screen | Admin → Driver Approval Page</p></td></tr><tr><td><p><strong>Priority</strong></p></td><td><p>CRITICAL — Fix before any release</p></td></tr><tr><td><p><strong>Summary</strong></p></td><td><p>Drivers are being redirected to the 'Under Review' screen before their documents (especially vehicle photo) have successfully uploaded to Supabase storage. Admin sees 'No File' for vehicle photo in the approval panel.</p></td></tr></table><p><strong>Problem Statement</strong></p><ul><li>The app navigates to the verification screen on form submission regardless of whether all file uploads to Supabase bucket have completed</li><li>Race condition: DB record is written before the storage upload resolves</li><li>Admin driver approval page shows 'No File' for vehicle photo because the file upload promise had not resolved when the record was saved</li><li>No retry or error handling if a single document upload fails</li></ul><p><strong>Developer Tasks</strong></p><ul><li>Refactor the submission flow: first upload ALL documents to Supabase storage bucket using Promise.all() — wait for ALL promises to resolve before proceeding</li><li>Only after ALL storage uploads succeed, write the driver record to the database with the resolved public URLs</li><li>If any upload fails, show an error toast with the specific document name that failed and a retry button — DO NOT submit the form</li><li>Add a loading state during the upload+submit phase: show 'Uploading documents... X of Y' progress feedback</li><li>After all uploads and DB write succeed, THEN navigate to the Verification Pending screen</li><li>In admin panel: add a null/empty check for document URLs and show 'Not Uploaded' with a red badge instead of silently showing nothing</li></ul><p><strong>Reference / Notes</strong></p><p><em>Critical flow fix. Use Promise.all([uploadDoc1, uploadDoc2, uploadDoc3]) → then save record. Do not use Promise.allSettled here — any failure should block submission.</em></p><p><strong>  BUG-007  </strong>  <strong>[HIGH]  Admin: Request Resubmission Button on Driver Verification Screen</strong></p><table><tr><td><p><strong>Module / Screen</strong></p></td><td><p>Admin Web Console → Driver Verification / Approval Screen</p></td></tr><tr><td><p><strong>Priority</strong></p></td><td><p>HIGH — Fix in next sprint</p></td></tr><tr><td><p><strong>Summary</strong></p></td><td><p>Admin currently can only Approve or Reject a driver. There is no option to request specific corrections or document resubmission with a message — forcing full rejection for minor issues.</p></td></tr></table><p><strong>Problem Statement</strong></p><ul><li>Admin has no way to communicate what needs to be fixed without rejecting outright</li><li>Driver has no context on what to resubmit if rejected</li><li>No 'Request Resubmission' CTA exists in the admin console</li></ul><p><strong>Developer Tasks</strong></p><ul><li>Add a 'Request Resubmission' button below the 'Approve Driver' button on the admin driver detail screen</li><li>When clicked, show a modal/drawer with a text area: 'Enter resubmission instructions for driver' (max 500 chars)</li><li>On confirm, update the driver's status in Supabase to 'resubmission_requested' and save the admin's message to a field (e.g., resubmission_note)</li><li>In the driver app: when status is 'resubmission_requested', show a dedicated screen with the admin's message prominently displayed and a 'Resubmit Documents' CTA</li><li>Send a push notification to the driver immediately when resubmission is requested</li><li>Log all admin actions (approve / reject / resubmit request) with timestamp and admin user ID in an audit table</li></ul><p><strong>Reference / Notes</strong></p><p><em>Driver statuses in Supabase: pending | under_review | resubmission_requested | approved | rejected | suspended | banned</em></p><p><strong>  BUG-008  </strong>  <strong>[HIGH]  Push Notifications to Driver on Approval, Rejection &amp; Resubmission</strong></p><table><tr><td><p><strong>Module / Screen</strong></p></td><td><p>Driver App → Notification System</p></td></tr><tr><td><p><strong>Priority</strong></p></td><td><p>HIGH — Fix in next sprint</p></td></tr><tr><td><p><strong>Summary</strong></p></td><td><p>Drivers who have submitted their documents and are waiting for approval receive no notification when their status changes. They must manually check the app.</p></td></tr></table><p><strong>Problem Statement</strong></p><ul><li>No push notification is triggered when driver is approved, rejected, or asked for resubmission</li><li>Driver experience: they submit documents, get no feedback, and lose trust in the platform</li></ul><p><strong>Developer Tasks</strong></p><ul><li>Set up a Supabase database trigger (or edge function) that fires when driver status changes in the drivers table</li><li>On status change, call the push notification service (Firebase FCM or Expo Notifications) with the driver's device token</li><li>Notification messages by status: Approved → 'You're approved! Start accepting rides on Cartr.', Rejected → 'Your application was not approved. Please check the app for details.', Resubmission → 'Action needed: Admin has requested changes to your documents. Tap to view.'</li><li>Store notification history in a notifications table in Supabase</li><li>In the driver app, show an in-app notification badge and notification center screen</li><li>Handle edge case: device token may be stale — implement token refresh on app open</li></ul><p><strong>Reference / Notes</strong></p><p><em>Use Expo Notifications for React Native cross-platform support. For Supabase triggers: use pg_notify + edge function or direct webhook.</em></p><p><strong>  BUG-009  </strong>  <strong>[CRITICAL]  Suspended Driver Must See a Proper Ban Screen (Not Verification Failed)</strong></p><table><tr><td><p><strong>Module / Screen</strong></p></td><td><p>Driver App → Post-Suspension Screen</p></td></tr><tr><td><p><strong>Priority</strong></p></td><td><p>CRITICAL — Fix before any release</p></td></tr><tr><td><p><strong>Summary</strong></p></td><td><p>When an admin suspends a driver, the driver app incorrectly shows the 'Verification Failed' screen. The driver has no idea they were suspended or why.</p></td></tr></table><p><strong>Problem Statement</strong></p><ul><li>Suspended driver is routed to verification failed screen — completely wrong context</li><li>Admin cannot currently set a suspension reason or duration (temporary vs permanent)</li><li>Driver sees confusing message not related to their actual situation</li><li>No distinction between temporary suspension and permanent ban in the app flow</li></ul><p><strong>Developer Tasks</strong></p><ul><li>Add suspension_type field to driver record: values — 'temporary' | 'permanent'</li><li>Add suspension_reason (text), suspension_until (timestamp, null if permanent) fields</li><li>In admin console: when suspending, show a form to select type (Temporary / Permanent), enter reason, and if temporary — pick an end date</li><li>In driver app: check driver status on app load — if 'suspended', route to a dedicated Suspension Screen (not verification failed)</li><li>Suspension screen should show: 'Your account has been suspended', the reason provided by admin, duration (if temporary: 'Until [date]'; if permanent: 'This suspension is permanent'), and a 'Contact Support' button</li><li>If suspension_until is in the past, automatically restore access and notify the driver</li><li>For permanent ban, use status 'banned' — show a 'You have been permanently banned' screen with no re-registration path</li></ul><p><strong>Reference / Notes</strong></p><p><em>Priority: this is a critical trust and compliance issue. Drivers receiving wrong messages could create legal/support escalations.</em></p><p><strong>  BUG-010  </strong>  <strong>[CRITICAL]  Driver App — Pickup Location Inaccurate on Map</strong></p><table><tr><td><p><strong>Module / Screen</strong></p></td><td><p>Driver App → Active Ride → Map View</p></td></tr><tr><td><p><strong>Priority</strong></p></td><td><p>CRITICAL — Fix before any release</p></td></tr><tr><td><p><strong>Summary</strong></p></td><td><p>After accepting a ride, the pickup location pin shown on the driver's map is incorrect or misaligned. Driver wastes time navigating to wrong location.</p></td></tr></table><p><strong>Problem Statement</strong></p><ul><li>Pickup coordinates passed to the map component may be lat/lng reversed</li><li>Customer's pickup location may be stored as a string instead of a parsed {lat, lng} object, causing incorrect pin placement</li><li>Map may be showing the driver's own location instead of the customer's pickup location</li></ul><p><strong>Developer Tasks</strong></p><ul><li>Audit the data flow: trace how customer pickup coordinates are stored in Supabase → fetched by driver app → passed to the map component</li><li>Verify coordinate format consistency (lat first, lng second) across all layers</li><li>Add console logging in dev build to confirm the exact coordinates being rendered on map</li><li>Test with known landmark coordinates in Pune to verify pin accuracy</li><li>If using Google Maps SDK: ensure the Marker position prop receives {latitude, longitude} not {lng, lat}</li><li>Add a visible coordinate display in debug mode to make QA easier</li></ul><p><strong>Reference / Notes</strong></p><p><em>Common bug: coordinates stored as [lng, lat] in PostGIS / GeoJSON format but read as [lat, lng] by React Native maps component.</em></p><p><strong>  BUG-011  </strong>  <strong>[HIGH]  Driver App Map Doesn't Show Directions / Route to Drop Location</strong></p><table><tr><td><p><strong>Module / Screen</strong></p></td><td><p>Driver App → Active Ride → Map View</p></td></tr><tr><td><p><strong>Priority</strong></p></td><td><p>HIGH — Fix in next sprint</p></td></tr><tr><td><p><strong>Summary</strong></p></td><td><p>After pickup, the driver app map does not show a mapped road route to the drop location. Driver has no in-app navigation.</p></td></tr></table><p><strong>Problem Statement</strong></p><ul><li>No route polyline is being drawn on the map between current location and drop location</li><li>Driver app may not be calling Google Directions API or equivalent to get route data</li><li>Map is likely showing only a straight line or no line at all</li></ul><p><strong>Developer Tasks</strong></p><ul><li>Integrate Google Directions API (or OSRM for a free alternative): call the API with origin = driver's current GPS coordinates, destination = drop location coordinates</li><li>Decode the polyline from the API response and render it as a Polyline component on the map</li><li>Update the route in real-time as driver's location changes (re-fetch or use live navigation SDK)</li><li>Show turn-by-turn direction summary at the top of the screen (optional but recommended)</li><li>Consider integrating deep-link to Google Maps / Waze as a fallback navigation option</li><li>Cache the route on ride accept so it loads instantly even on poor connectivity</li></ul><p><strong>Reference / Notes</strong></p><p><em>Google Directions API pricing: ~$5 per 1000 requests — acceptable for production. Alternatively use react-native-maps-directions package.</em></p><p><strong>  BUG-012  </strong>  <strong>[CRITICAL]  QR Code After Ride Redirects to Supabase URL — Must Use CashFree In-App Payment</strong></p><table><tr><td><p><strong>Module / Screen</strong></p></td><td><p>Driver App → Ride Completion → Payment Flow</p></td></tr><tr><td><p><strong>Priority</strong></p></td><td><p>CRITICAL — Fix before any release</p></td></tr><tr><td><p><strong>Summary</strong></p></td><td><p>After a ride completes, the driver app shows a QR code that, when scanned, opens a raw Supabase URL. This is a completely broken payment experience. No real payment processing is happening.</p></td></tr></table><p><strong>Problem Statement</strong></p><ul><li>QR code encodes a Supabase storage or table URL — this is a development artifact that shipped to production</li><li>No real payment gateway integration exists for the ride payment flow</li><li>Driver has no way to confirm receipt of payment through the app</li><li>Customer scanning the QR has no idea what to do or what amount to pay</li></ul><p><strong>Developer Tasks</strong></p><ul><li>REMOVE the QR code entirely from the ride completion screen</li><li>Integrate Cashfree Payment Gateway: use Cashfree's React Native SDK for in-app UPI/card payments</li><li>Payment flow: Driver taps 'Collect Payment' → App creates a Cashfree order via API (amount, order_id, customer details) → Launch Cashfree checkout inside the app → On payment success callback, update ride status to 'payment_received' in Supabase → Show success screen to driver</li><li>Show the payment amount clearly on the pre-payment screen: 'Collect ₹[amount] from [customer name]'</li><li>Support both: in-app Cashfree payment AND a 'Mark as Cash Collected' option for cash rides</li><li>Never redirect user out of the app for payment — use Cashfree's in-app SDK not a webview redirect</li><li>Test with Cashfree sandbox before going live</li></ul><p><strong>Reference / Notes</strong></p><p><em>Cashfree React Native SDK docs: https://docs.cashfree.com/docs/react-native-integration — Use PG_ENV_SANDBOX for testing</em></p><p><strong>  BUG-013  </strong>  <strong>[HIGH]  OTP Verification Takes Too Long — Add Retry Logic and UX Improvements</strong></p><table><tr><td><p><strong>Module / Screen</strong></p></td><td><p>Driver App → OTP Verification Screen</p></td></tr><tr><td><p><strong>Priority</strong></p></td><td><p>HIGH — Fix in next sprint</p></td></tr><tr><td><p><strong>Summary</strong></p></td><td><p>OTP verification is slow and the user has no feedback during the delay. There is no retry option if OTP fails, and no timeout handling.</p></td></tr></table><p><strong>Problem Statement</strong></p><ul><li>OTP verification API call takes too long with no loading indicator</li><li>If OTP times out or fails, the user is stuck with no actionable option</li><li>No 'Resend OTP' functionality with countdown timer</li><li>No limit on incorrect OTP attempts</li></ul><p><strong>Developer Tasks</strong></p><ul><li>Add a visible loading spinner/progress indicator immediately when user submits OTP</li><li>Set a timeout of 30 seconds on the OTP verification API call — show an error if exceeded: 'Verification is taking longer than expected. Please try again.'</li><li>Add 'Resend OTP' button with a 30-second countdown timer before it becomes active</li><li>Implement retry logic: auto-retry the OTP API call up to 2 times on network failure before showing an error</li><li>Add rate limiting: lock the screen after 5 failed OTP attempts with message: 'Too many attempts. Please request a new OTP.'</li><li>Use optimistic UI: auto-advance when 6th digit is entered (don't require manual 'Verify' button tap)</li></ul><p><strong>Reference / Notes</strong></p><p><em>For Supabase OTP: the verifyOtp() function should be wrapped in a Promise with AbortController for timeout control</em></p><p><strong>  BUG-014  </strong>  <strong>[HIGH]  Post-Delivery OTP Accepted But App Stuck in Loading State</strong></p><table><tr><td><p><strong>Module / Screen</strong></p></td><td><p>Driver App → Delivery OTP → Trip Completion Screen</p></td></tr><tr><td><p><strong>Priority</strong></p></td><td><p>HIGH — Fix in next sprint</p></td></tr><tr><td><p><strong>Summary</strong></p></td><td><p>After the driver enters the delivery OTP and it is accepted, the app stays in a loading/processing state indefinitely and does not transition to the 'Trip Completed' confirmation.</p></td></tr></table><p><strong>Problem Statement</strong></p><ul><li>The OTP is verified successfully but the subsequent state update (marking trip complete) silently fails or is not awaited correctly</li><li>No error handling after OTP success — if trip completion API call fails, user sees infinite loading</li><li>Driver cannot exit this state without force-closing the app</li></ul><p><strong>Developer Tasks</strong></p><ul><li>After OTP verification succeeds, ensure the trip completion mutation is properly awaited and its success/error is handled</li><li>Add explicit error handling after OTP success: if trip completion fails, show error toast and allow retry</li><li>Set a maximum loading time of 15 seconds — if not completed, show: 'Something went wrong. Tap to retry.'</li><li>Add a 'force complete' fallback for edge cases (with logging for admin review)</li><li>Test the full flow end-to-end: OTP entry → verify → mark trip complete → payment screen → home</li></ul><p><strong>Reference / Notes</strong></p><p><em>Likely cause: Promise chain not properly awaited after verifyOtp() resolves, or Supabase update call missing await</em></p><p><strong>  BUG-015  </strong>  <strong>[MEDIUM]  Driver Rides Tab — Show Ride History + Ongoing Rides + New Requests</strong></p><table><tr><td><p><strong>Module / Screen</strong></p></td><td><p>Driver App → Rides / Requests Tab</p></td></tr><tr><td><p><strong>Priority</strong></p></td><td><p>MEDIUM — Fix within 2 sprints</p></td></tr><tr><td><p><strong>Summary</strong></p></td><td><p>The driver's ride request tab only shows new incoming requests. It does not show ongoing rides or past ride history, making it impossible for drivers to track their work.</p></td></tr></table><p><strong>Problem Statement</strong></p><ul><li>No ride history section on the rides tab</li><li>Ongoing/active ride does not appear in the rides tab</li><li>Driver cannot distinguish between new requests, active ride, and completed rides in one view</li></ul><p><strong>Developer Tasks</strong></p><ul><li>Redesign the Rides tab with three sections: (1) New Requests at top — real-time incoming ride requests with Accept/Decline CTAs, (2) Ongoing Ride — currently active ride with navigation CTA, (3) Ride History — paginated list of completed rides with date, route, amount</li><li>Use tab segments or a vertical scroll with sticky section headers</li><li>Fetch ride history from Supabase with pagination (20 per page, load more on scroll)</li><li>Show ride history card: pickup → drop, date/time, fare earned, ride status badge</li><li>New ride requests should have a countdown timer showing time remaining to accept</li></ul><p><strong>Reference / Notes</strong></p><p><em>Priority: MEDIUM — important for driver satisfaction but does not block core functionality</em></p><p><strong>  BUG-016  </strong>  <strong>[HIGH]  Customer App Shows 'Shipment In Progress' After Trip Completion</strong></p><table><tr><td><p><strong>Module / Screen</strong></p></td><td><p>Customer App → Track Shipment Screen</p></td></tr><tr><td><p><strong>Priority</strong></p></td><td><p>HIGH — Fix in next sprint</p></td></tr><tr><td><p><strong>Summary</strong></p></td><td><p>Even after a ride is marked as completed, the customer app continues to show 'Shipment In Progress' on the tracking page. The UI state is not updating to reflect trip completion.</p></td></tr></table><p><strong>Problem Statement</strong></p><ul><li>Customer app is not subscribing to real-time trip status updates from Supabase</li><li>Trip status change (in_progress → completed) is not reflected in the customer UI</li><li>Customer may rebook or raise a support complaint thinking the ride is still active</li></ul><p><strong>Developer Tasks</strong></p><ul><li>Implement a Supabase real-time subscription on the trip/ride record in the customer app — listen for status changes</li><li>When status changes to 'completed', immediately update the UI to show 'Trip Completed' screen with summary (fare, route, driver details)</li><li>Add a polling fallback (every 10 seconds) in case the real-time subscription drops</li><li>Test the full lifecycle: booking → accepted → pickup → in_progress → completed — verify UI updates at each stage</li><li>Clear the active ride context/state in customer app on completion to prevent stale data</li></ul><p><strong>Reference / Notes</strong></p><p><em>Use Supabase real-time: supabase.channel('trip').on('UPDATE', handler).subscribe() — filter by trip ID</em></p><p><strong>  BUG-017  </strong>  <strong>[HIGH]  Previous Trip Details Pre-filled When Starting New Booking</strong></p><table><tr><td><p><strong>Module / Screen</strong></p></td><td><p>Customer App → New Booking → Select Location / Receiver Details Screen</p></td></tr><tr><td><p><strong>Priority</strong></p></td><td><p>HIGH — Fix in next sprint</p></td></tr><tr><td><p><strong>Summary</strong></p></td><td><p>When a customer completes a trip and starts a new ride request, the pickup, drop, and receiver details from the previous trip are pre-filled in the form, causing confusion and potential mis-bookings.</p></td></tr></table><p><strong>Problem Statement</strong></p><ul><li>Trip/booking form state is not reset after a trip is completed</li><li>Customer may not notice pre-filled details and accidentally book a duplicate/old route</li><li>Receiver details (name, phone) from previous booking persist into new booking form</li></ul><p><strong>Developer Tasks</strong></p><ul><li>On trip completion (status = 'completed'), explicitly reset the booking form state to empty defaults</li><li>Clear the active booking context/store (Redux/Zustand) on trip completion</li><li>Navigate customer to the home/new booking screen after completion with a fresh state</li><li>Optional UX: offer 'Book same route again?' as a prompt after completion — but make it opt-in, not default</li><li>Add a unit test to verify form state is empty when navigating to new booking from completion screen</li></ul><p><strong>  BUG-018  </strong>  <strong>[HIGH]  Driver App — No Floating Notification for New Ride Requests</strong></p><table><tr><td><p><strong>Module / Screen</strong></p></td><td><p>Driver App → Background / Foreground Notification</p></td></tr><tr><td><p><strong>Priority</strong></p></td><td><p>HIGH — Fix in next sprint</p></td></tr><tr><td><p><strong>Summary</strong></p></td><td><p>When a new ride request comes in, the driver does not see a floating heads-up notification (foreground notification overlay). They miss ride requests if not actively watching the app.</p></td></tr></table><p><strong>Problem Statement</strong></p><ul><li>Foreground push notifications are not configured — FCM/Expo notifications only work in background</li><li>No heads-up notification banner shown while app is open</li><li>Driver misses ride requests silently</li></ul><p><strong>Developer Tasks</strong></p><ul><li>Configure FCM/Expo Notifications for HIGH priority foreground notifications on Android (requires IMPORTANCE_HIGH notification channel)</li><li>Use expo-notifications setNotificationHandler to show notifications even when app is foregrounded</li><li>On new ride request: trigger a high-priority push notification with sound + vibration</li><li>In-app: show a floating bottom sheet / modal with ride request details (pickup, drop, fare, timer to accept)</li><li>Notification should include: pickup area, drop area, estimated fare, time to respond (countdown)</li><li>If driver does not respond within the time window, auto-decline and notify</li></ul><p><strong>Reference / Notes</strong></p><p><em>Android requires a notification channel with IMPORTANCE_HIGH for heads-up notifications. Set this up during app initialization.</em></p><p><strong>  BUG-019  </strong>  <strong>[HIGH]  Accepting Ride from Notification Does Not Show Route in App</strong></p><table><tr><td><p><strong>Module / Screen</strong></p></td><td><p>Driver App → Notification → In-App Ride View</p></td></tr><tr><td><p><strong>Priority</strong></p></td><td><p>HIGH — Fix in next sprint</p></td></tr><tr><td><p><strong>Summary</strong></p></td><td><p>When a driver taps 'Accept' on a ride request notification, the app opens but does not show the accepted ride details or route. The driver is left on a blank or home screen.</p></td></tr></table><p><strong>Problem Statement</strong></p><ul><li>Deep link from notification to app does not pass the ride/request ID</li><li>App opens but does not navigate to the active ride screen</li><li>No accepted ride shown in the rides tab after accepting from notification</li></ul><p><strong>Developer Tasks</strong></p><ul><li>Implement deep linking: notification payload should include ride_request_id</li><li>On notification tap, use Linking or expo-notifications response handler to navigate to the ActiveRide screen with the correct ride_request_id</li><li>Fetch and display the accepted ride details (pickup, drop, map route) immediately on navigation</li><li>Update the rides tab to show the newly accepted ride under 'Ongoing' section</li><li>Test: accept ride from notification while app is in background, foreground, and killed state — all three must correctly navigate to the ride screen</li></ul><p><strong>Reference / Notes</strong></p><p><em>Use expo-notifications addNotificationResponseReceivedListener for handling taps on notifications</em></p><p><strong>  BUG-020  </strong>  <strong>[CRITICAL]  Payment Completion Flow Needs Multi-Step Confirmation — Edge Case Risk</strong></p><table><tr><td><p><strong>Module / Screen</strong></p></td><td><p>Driver App → Drop Location → Payment Collection → Trip Completion</p></td></tr><tr><td><p><strong>Priority</strong></p></td><td><p>CRITICAL — Fix before any release</p></td></tr><tr><td><p><strong>Summary</strong></p></td><td><p>Currently, tapping 'Complete and Collect Payment' immediately marks the trip as completed and assumes payment was received. This is a critical business logic flaw — drivers could accidentally complete trips without collecting payment.</p></td></tr></table><p><strong>Problem Statement</strong></p><ul><li>Single tap completes the trip AND marks payment as received — no confirmation step</li><li>If driver taps button accidentally, the trip is completed and customer is charged with no recourse</li><li>No verification that payment was actually received before trip is marked complete</li><li>This flow differs from industry standard (Porter, Swiggy Genie, etc. all have multi-step confirmation)</li></ul><p><strong>Developer Tasks</strong></p><ul><li>Redesign the drop-location flow as a multi-step process: Step 1 — Driver arrives at drop location: show 'I've Arrived' button. Step 2 — Delivery confirmation: driver must enter customer OTP or customer confirms receipt. Step 3 — Payment collection: show payment amount + options (Cashfree in-app / Cash). Step 4 — Payment confirmed: only after payment success callback (or explicit 'Cash Collected' confirmation with double confirmation dialog) → mark trip as complete. Step 5 — Trip Complete: show summary screen.</li><li>Add a confirmation dialog before any irreversible action: 'Are you sure you want to mark this trip as complete? This cannot be undone.'</li><li>For cash payments: show 'Confirm Cash Received' → secondary confirmation: 'You are confirming receipt of ₹[amount] in cash. Confirm?'</li><li>Log all payment events with timestamp, method, and driver confirmation in Supabase</li><li>Study Porter / Dunzo / Swiggy Genie flows — all require explicit payment confirmation before trip close</li><li>Add a 'Raise Issue' option for cases where payment was not received despite trip being marked complete</li></ul><p><strong>Reference / Notes</strong></p><p><em>Reference flows: Porter (cash confirmation modal), Dunzo (OTP + payment before completion). This is a revenue-critical bug.</em></p>